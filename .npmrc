# Avoid OpenTrader's default postinstall (seed + Prisma) during npm's install pass — it fails on Windows.
# Run `npm run setup:deps` after install, or use `npm run reinstall` for a full clean install.
ignore-scripts=true
